version: '3'
services:
  pond:
    image: "prawnalith/pond:latest"
    ports:
      - "443:8000"
    volumes:
      - "/var/volumes/pond:/data"
    depends_on:
      - "redis"
  redis:
    image: "redis"
    volumes:
      - "/var/volumes/redis:/data"
